# Контракты между доменами

## Статус

Черновик. Контракты ниже описаны по постановке задачи и зафиксированы в
коде как интерфейсы + заглушки, чтобы никто не ждал чужой реализации.
Финальное подтверждение — созвон троих (Bekzat, Дарья, Анель): все должны
одинаково понимать границы, до того как M1 начнётся всерьёз.

## Зачем

Домены поделены так, чтобы пересекаться минимально, но четыре точки стыка
есть в любом случае. Если их не описать до начала M1 — они превратятся во
взаимные блокировки в середине спринта. Договариваемся об интерфейсах на
бумаге и в коде, до того как заработала реальная логика с обеих сторон.

## Правило изменений

У каждого контракта один владелец. Менять сигнатуру или семантику статусов
можно только через PR, где владелец — обязательный ревьюер. Потребитель
кодирует свою сторону против интерфейса (`ABC`) и заглушки (`Stub...`) ниже,
а не против внутренностей чужой реализации — когда реальный сервис будет
готов, потребитель меняет только импорт (`Stub...Service` → `...Service`),
не свой код вокруг вызова.

## Сводка

| #   | Контракт | Владелец | Потребитель(и) | Код |
| --- | -------- | -------- | -------------- | --- |
| 1   | Расписание → Деньги: `SubscriptionService.consume` | Bekzat | Дарья (посещаемость) | [`backend/domains/money/subscriptions/subscription_service.py`](../domains/money/subscriptions/subscription_service.py) |
| 2   | Люди → Деньги и Продажи: `ChildService.find_duplicates` / `create_with_parent` / `link_parent` | Анель | импорт Excel, конвертация заявки | [`backend/domains/people/clients/services.py`](../domains/people/clients/services.py) |
| 3   | Расписание → всем: `LessonService.enroll` | Дарья | отработки (M1), пробные (M2) | `backend/apps/schedule/services.py` (путь уточнит Дарья) |
| 4   | Деньги → всем: `AuditLog.record` | Bekzat | все домены | [`backend/domains/platform/core/audit.py`](../domains/platform/core/audit.py) |


## 1. Расписание → Деньги

```python
SubscriptionService.consume(child_id: int, lesson_id: int) -> ConsumeResult
```

Вызывается при отметке посещения «пришёл» — списывает занятие с активного
абонемента ребёнка. `ConsumeResult.status` различает четыре исхода:
`CONSUMED`, `NO_ACTIVE_SUBSCRIPTION`, `SUBSCRIPTION_EXHAUSTED`,
`RULE_FORBIDS_CONSUMPTION`. Все четыре — ожидаемые бизнес-исходы, не
исключения; что делать с `NO_ACTIVE_SUBSCRIPTION` (визуальный флаг + задача
администратору, ТЗ п. 4.3) решает потребитель, а не сервис.

Владелец: **Bekzat**. Потребитель: **Дарья**.

## 2. Люди → Деньги и Продажи

```python
ChildService.find_duplicates(
    organization, *, phone=None, child_name=None, birth_date=None
) -> list[DuplicateMatch]          # DuplicateMatch(reason, child, parent)

ChildService.create_with_parent(
    organization, *, child_data: dict, parent_data: dict, link_role: str
) -> Child                         # parent_data={"id": ...} — существующий родитель,
                                   # иначе {"full_name", "phones": [...]} — новый

ChildService.link_parent(
    organization, child, *, parent_data: dict, link_role: str
) -> (ChildContact, ParentContact, created: bool)   # контакт к уже существующему ребёнку
```

`DuplicateMatch.reason` — на каком основании совпадение
(`DuplicateReason`): `phone` (тот же телефон и тот же ребёнок),
`existing_parent_new_child` (телефон есть, ребёнок другой — это второй
ребёнок в семье, не дубль: привязывать к существующему родителю),
`name_and_birth_date` (сильное), `name_only` (слабое). Автоматического
слияния нет — решает человек.

Нужно и для импорта Excel (ТЗ п. 4.1 — дубли выявляются при импорте), и для
конвертации заявки (ТЗ п. 5.1 — «Пришёл на пробное» создаёт или связывает
существующего ребёнка). Поиск дублей — обязанность вызывающей стороны:
сначала `find_duplicates`, затем осознанное решение создавать новую запись
или связать с найденной.

**Плательщик и контакты** (связь `ChildContact`, ТЗ п. 1.2.1):

- У ребёнка не больше **одного** активного плательщика одновременно
  (`is_payer`) и одного основного контакта (`is_primary_contact`);
  назначение нового автоматически снимает флаг с предыдущего. Плательщик
  и основной контакт могут быть разными людьми.
- Отвязка контакта — мягкое удаление связи, не родителя и не ребёнка.
- Оплата (`Payment`) сегодня ссылается на абонемент, а не на родителя,
  поэтому отвязка оплаты не удаляет. Но «кто платил» по конкретной оплате
  не хранится — для истории оплат родителя после смены плательщика нужно
  поле `Payment.payer → ParentContact` (зона Bekzat).

Владелец: **Анель**. Потребители: импорт Excel, домен «Продажи».

## 3. Расписание → всем

```python
LessonService.enroll(lesson_id: int, child_id: int, kind: EnrollKind) -> EnrollResult
```

`kind = MAKEUP | TRIAL` — один механизм обслуживает и отработки (M1), и
пробные занятия (M2). Контроль вместимости обязателен: переполненное
занятие — это `LESSON_FULL`, а не исключение. Право ребёнка на отработку
(есть ли абонемент, не истёк ли срок) сервис не проверяет — это делает
вызывающая сторона до вызова `enroll`.

Владелец: **Дарья**. Потребители: все домены (в первую очередь «Деньги» —
отработки, «Продажи» — пробные).

## 4. Деньги → всем

```python
AuditLog.record(actor, action: str, entity: AuditEntity, before: dict | None, after: dict | None) -> None
```

Один общий сервис вместо трёх копий в трёх доменах (ТЗ п. 2 — аудит-лог
изменений оплат, посещений, абонементов). `action` — namespaced строка вида
`"domain.verb"` (`"payments.update"`, `"attendance.edit_backdated"`);
реестр допустимых действий ведут вызывающие домены, не владелец интерфейса.
Код лежит в `apps/core`, а не в `apps/payments` — сервис общий, хотя
интерфейс принадлежит домену «Деньги».

Владелец: **Bekzat**. Потребители: все домены.
